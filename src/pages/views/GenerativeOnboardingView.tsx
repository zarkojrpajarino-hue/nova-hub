/**
 * GenerativeOnboardingView — stub
 * El componente completo está pendiente de implementación.
 */

export function GenerativeOnboardingView() {
  const { t } = useTranslation();
  return (
    <div className="flex items-center justify-center py-16 text-muted-foreground text-sm">{t('generativeOnboarding.generativeOnboardingPróximamente')}</div>
  )
}
